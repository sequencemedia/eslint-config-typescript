import {
  expect
} from 'chai'

import recommended from '@sequencemedia/eslint-config-typescript/configs/recommended'

describe('@sequencemedia/eslint-config-typescript/configs/recommended', () => {
  describe('`recommended`', () => it('is an object', () => expect(recommended).to.be.an('object')))
})
