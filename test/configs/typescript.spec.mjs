import {
  expect
} from 'chai'

import typescript from '@sequencemedia/eslint-config-typescript/configs/typescript'

describe('@sequencemedia/eslint-config-typescript/configs/typescript', () => {
  describe('`typescript`', () => it('is an object', () => expect(typescript).to.be.an('object')))
})
