import {
  expect
} from 'chai'

import stylistic from '@sequencemedia/eslint-config-typescript/configs/stylistic'

describe('@sequencemedia/eslint-config-typescript/configs/stylistic', () => {
  describe('`stylistic`', () => it('is an object', () => expect(stylistic).to.be.an('object')))
})
