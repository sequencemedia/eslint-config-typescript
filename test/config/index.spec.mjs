import {
  expect
} from 'chai'

import { typescript } from '@sequencemedia/eslint-config-typescript/config'

describe('@sequencemedia/eslint-config-typescript/config', () => {
  describe('`typescript`', () => it('is an object', () => expect(typescript).to.be.an('object')))
})
